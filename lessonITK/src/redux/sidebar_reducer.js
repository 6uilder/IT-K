


let initialState = {
    friends: [
        { name: 'Pushkin', id: '1' },
        { name: 'Tolstoy', id: '2' },
        { name: 'Fet', id: '3' }
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state;
}


export default sidebarReducer;